import { ImgbrokenDirective } from './imgbroken.directive';

describe('ImgbrokenDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgbrokenDirective();
    expect(directive).toBeTruthy();
  });
});
